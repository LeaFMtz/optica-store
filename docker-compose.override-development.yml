services:
  app:
    build:
      context: .
      target: dev
      args:
        WITH_DEV_DEPS: "true"
    environment:
      - DB_CONNECTION=${DB_CONNECTION}
      - APP_ENV=${APP_ENV}
      - WITH_ASSETS=empty_assets
    ports:
      - "5173:5173"
    volumes:
      - .:/var/www/html:z
